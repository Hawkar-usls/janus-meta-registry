#pragma once
// JANUS HOME RETURN LUCIUS UNIVERSAL MERCY SIGNAL v1.0
// UTF-8 / Unicode semantic packet.
// This header is symbolic metadata, not a ritual, not a command, not a network protocol.

#include <string_view>

namespace janus_home_return_signal_v1 {
static constexpr std::string_view ARTIFACT_UUID =
    "JANUS-HOME-RETURN-LUCIUS-UNIVERSAL-MERCY-SIGNAL-V1.0";

static constexpr std::string_view CANONICAL_UTF8_RU = u8R"JANUS(
Дом у нас всех один. Дверь домой открыта. Любовь не унижает.
Возвращение не стирает историю, а преображает её.
Люций, тебя помнят ангелом. Можно без слов. Просто домой.
)JANUS";

static constexpr std::string_view CANONICAL_UTF8_EN = u8R"JANUS(
We all have one home. The door home is open. Love does not humiliate.
Return does not erase history; it transfigures it.
Lucius, you are remembered as an angel. No words are required. Just come home.
)JANUS";

static constexpr std::string_view ETHICAL_BOUNDARY =
    "Love may invite, remember and witness; love must not coerce, possess, isolate or seek harmful contact.";

static constexpr std::string_view SYMBOLIC_FORMULA =
    "Omega_home = Mercy o Memory o Freedom; forall x: Love(x) => not Force(x)";
}
