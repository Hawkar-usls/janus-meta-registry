# JANUS Not-Prediction — C030–C035 Atomic Overlay v1.4

The six candidate records from the v1.1 re-audit were re-evaluated against the frozen event-root ontology rather than added mechanically to the historical 29-case count.

## Result

The validated baseline remains:

```text
29 historical semantic families
24 strict phenomenon/session roots
3 uncertain phenomenology roots
27 inclusive roots
```

The six overlay candidates produce:

```text
C030 04:40 Chaos/Harmony     -> +1 strict
C031 Storm Time-Traveler     -> +1 strict
C032 03:03 Holy Clock        -> +0, already baseline NP-003
C033 21:27 Gorodok           -> +1 strict
C034 Elian pre-identification-> +1 uncertain
C035 Aura/Kem continuity     -> +0, reference/provenance only
```

Therefore the expanded accounting is:

```text
27 strict phenomenon/session roots
4 uncertain phenomenology roots
31 inclusive roots
```

**31 is not a prediction count.** It is an inclusive accounting count of strict plus low-resolution observation/phenomenology roots across the baseline and the reviewed overlay.

## Why six candidates become four root additions

`C032` is duplicate at the event-root level. The `03:03` session already exists inside the twelve-event normalization of `NP-003`. A new candidate label cannot create a second event.

`C035` proves useful chronology: Aura Oracle existed in 2025 and already contained an oracle-themed creative workflow. The later Kem/Set link is retrospective continuity. Chronology is a reference edge, not a phenomenology event.

The invariant is:

```text
OVERLAY_CANDIDATE_COUNT != EVENT_ROOT_DELTA
```

## Strict additions

### C030 — 04:40

The Chaos/Harmony artifact preserves a concrete `04:40` observation with an explicit not-prediction boundary. It is a new clock-event root relative to the baseline.

It is **not statistically independent** of the other clock records: the archive is salience-triggered and same-number textual passages are selected after the time is noticed.

### C031 — Storm Time-Traveler

The August 7 timing experiment is one concrete experiment/session root. Negative Git offsets and arrival-order inversions remain observations inside that session. The trials share scheduler, automation, Git timestamp and normalization failure domains.

```text
MANY_TRIALS_IN_ONE_EXPERIMENT != MANY_INDEPENDENT_PHENOMENON_ROOTS
```

The source itself rejects physical retrocausality.

### C033 — 21:27 Gorodok

The recorded `21:27` is one new clock-event root. The later Quran/Bible `21:27` passages are lookup-conditioned presentations selected by the already observed coordinate.

```text
ONE_CLOCK_OBSERVATION + FIVE_LOOKUPS != SIX_WITNESSES
```

## Inclusive-only addition

### C034 — Elian

The archive preserves the autobiographical testimony that attraction preceded acquaintance/identification. It remains meaningful personal testimony, but the inspected record does not provide a contemporaneous pre-relationship timestamp or independent frozen receipt sufficient for strict event-root admission.

It is therefore retained rather than discarded, but classified as **low-resolution phenomenology**.

## Hidden structure strengthened by the overlay

The overlay reinforces a pattern that is more mundane but more defensible than precognition:

```text
EARLIER STATE
  -> PRESERVATION / MEMORY
  -> LATER CONTEXT
  -> RETROSPECTIVE COMPARISON
  -> CLAIM CEILING
```

The most apparently uncanny cases also depend most strongly on observation infrastructure:

- clocks depend on salience and lookup selection;
- Storm depends on scheduler/Git/automation semantics;
- autobiographical attraction depends on memory and later narrative reconstruction;
- Aura/Kem depends on repository chronology plus later creative incorporation.

That does not make the experiences meaningless. It tells us what a serious null model must reproduce.

## Claim ceiling

```text
27 strict roots != 27 predictions
31 inclusive roots != 31 predictions
chronology != foreknowledge
negative Git delta != backward causation
personal pre-identification memory != established precognition
symbolic clock coordinate != divine timetable
```

Meaning is preserved; extraordinary causation is not promoted.
